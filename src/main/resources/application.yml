server:
  port: 8081

spring:
  h2:
    console:
      enabled: true

keycloak:
  realm: keycloakdemo
  auth-server-url: http://localhost:8080/auth
  resource: springboot-keycloak
  public-client: true
  bearer-only: true